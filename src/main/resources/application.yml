spring:
  application:
    name: "product-service"
  profiles:
    active: dev #${PRODUCT_PROFILE:dev}

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: never

logging:
  level:
    root: INFO
